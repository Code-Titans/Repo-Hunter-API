#import Comment from "./comment.graphql"

 type Repo {
    id: ID!
    repo: String!
    repoLink: String
    description: String
    image: String
    comments: [UserComment]
    likes:Int
    owner: User
}
