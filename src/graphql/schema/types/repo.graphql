#import Comment from "./comment.graphql"

 type Repo {
    id: ID!
    repo: String!
    repoLink: String
    description: String
    comments: [Comment]
    likes:Int
    owner: User
}

type RepoLikes {
   repoId: Int
   liked: Boolean
   totalLikes: Int
}
