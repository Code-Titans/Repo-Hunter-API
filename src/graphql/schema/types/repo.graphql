#import Comment from "./comment.graphql"

 type Repo {
    id: ID!
    imageUrl: String
    description: String
    comments: Int
    likes:Int
    title: String
    starsGazers: Int
}

type RepoLikes {
   repoId: Int
   liked: Boolean
   totalLikes: Int
}
