#import Comment from "./comment.graphql"

 type Repo {
    id: ID!
    description: String
    title: String
    starsGazers: Int
}

type RepoLikes {
   repoId: Int
   liked: Boolean
   totalLikes: Int
}
